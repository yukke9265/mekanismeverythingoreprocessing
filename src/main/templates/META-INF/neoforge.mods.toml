modLoader="javafml"
loaderVersion="${loader_version_range}"
license="${mod_license}"

[[mods]]
modId="${mod_id}"
version="${mod_version}"
displayName="${mod_name}"
authors="yukke9265"
description='''
Mekanism joke addon: turn anything into "Everything Raw Ore", run it through the ore processing chain, and restore it.
'''

[[dependencies.${mod_id}]]
    modId="neoforge"
    type="required"
    versionRange="[${neo_version},)"
    ordering="NONE"
    side="BOTH"

[[dependencies.${mod_id}]]
    modId="minecraft"
    type="required"
    versionRange="${minecraft_version_range}"
    ordering="NONE"
    side="BOTH"

# Mekanism is required; load after it so its registries (chemicals etc.) exist.
[[dependencies.${mod_id}]]
    modId="mekanism"
    type="required"
    versionRange="${mekanism_version_range}"
    ordering="AFTER"
    side="BOTH"

# JEI is optional; used only to show special crafting recipes.
[[dependencies.${mod_id}]]
    modId="jei"
    type="optional"
    versionRange="[19.44.0,)"
    ordering="NONE"
    side="CLIENT"
